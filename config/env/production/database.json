{
    "defaultConnection": "default",
    "connections": {
      "default": {
        "connector": "bookshelf",
        "settings": {
          "client": "postgres",
          "host": "${process.env.DATABASE_HOST || 'strapi-database.cmyfdp47lvoo.us-east-1.rds.amazonaws.com'}",
          "port": "${process.env.DATABASE_PORT || 5432}",
          "database": "${process.env.DATABASE_NAME || 'strapi'}",
          "username": "${process.env.DATABASE_USERNAME || 'postgres'}",
          "password": "${process.env.DATABASE_PASSWORD || 'sandbox-layer5'}"
        },
        "options": {
          "ssl": false
        }
      }
    }
  }